program uses 3 types of datastructures

a Fifo queue - > holds mailbox data

a Linked List -> to hold the ACL

a Skip_list -> Average logn performance search for mailboxes.

import system calls to the kernel


